//import { getWormhole } from "@/services"
//import { toNative } from "@wormhole-foundation/sdk"

export const case1 = async () => {
    // const wh = await getWormhole("Testnet")
    // const solana = wh.getChain("Solana")
    // const tokenBridge = await solana.getTokenBridge()

    // const atops = wh.getChain("Aptos")
    // const atopsTokenBridge = await atops.getTokenBridge()
    // const native = await tokenBridge.getWrappedNative()
    // const token = await atopsTokenBridge.getWrappedAsset({
    //     address: native.toUniversalAddress(),
    //     chain: "Solana"
    // })
    // console.log(toNative("Aptos", token.toString()).toString())
}